var arr = [];

fn fib(x) {
    if x < 1 {
        return 1;
    }

    if len(arr) > x {
        return arr[x];
    }

    var val = fib(x - 2) + fib(x - 1);

    arr = arr + val;

    return val;
}

assert fib(35) == 24157817;
